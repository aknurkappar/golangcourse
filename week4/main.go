package main

import (
	"main/gormDatabase"
)

func main() {
	//sqlDatabase.Run()
	gormDatabase.Run()

	//gormDatabase.CreateUser(gormDB, "Ainur", 22, "Bio for Ainur", "profile.jpg")
	//age := 90
	//gormDatabase.GetUsers(gormDB, &age)
	//gormDatabase.UpdateUser(gormDB, 3, "Aknur", 21)
	//gormDatabase.GetProfile(gormDB, 2)
	//gormDatabase.GetProfile(gormDB, 3)
	//gormDatabase.UpdateProfile(gormDB, 2, "Updated Bio for Ainur", "new_photo2.jpg")
	//gormDatabase.DeleteUser(gormDB, 2)

	//"encoding/json"
	//"net/http"
}
